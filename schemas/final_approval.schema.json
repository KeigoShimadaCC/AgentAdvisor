{
  "$defs": {
    "FinalDecision": {
      "enum": [
        "accept",
        "revise"
      ],
      "title": "FinalDecision",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "approved_at": {
      "format": "date-time",
      "title": "Approved At",
      "type": "string"
    },
    "approved_by": {
      "minLength": 1,
      "title": "Approved By",
      "type": "string"
    },
    "decision": {
      "$ref": "#/$defs/FinalDecision"
    },
    "note": {
      "default": "",
      "title": "Note",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "minimum": 1,
      "title": "Schema Version",
      "type": "integer"
    }
  },
  "required": [
    "decision",
    "approved_by",
    "approved_at"
  ],
  "title": "FinalApproval",
  "type": "object"
}
